//

function oneplusavgsum(x,y)
{
    let c = 1 + (x+y)/2
    return c;
}
 let a=10
 let b=10
 console.log(" One plus average of sum is "+oneplusavgsum(a,b))

 //arrowfunction

 const sum = (p,q)=>{
    return p+q;
 }